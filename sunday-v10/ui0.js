var esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function hero(){const t=S.state.league[S.state.coach.team],s=S.state.season;return `<div class="hero"><div class="eyebrow">${esc(S.state.coach.role)} · ${esc(t.city)} ${esc(t.nick)}</div><h1>${s.phase==='REGULAR'?`Week ${s.week} · ${S.DAYS[s.day]}`:esc(s.phase)}</h1><div class="small muted">${esc(S.record(t))} · Reputation ${Math.round(S.state.coach.reputation)} · Trust ${Math.round(S.state.morale.coachTrust)}</div></div>`}
function decision(){const d=S.state.decisions[0];if(!d)return'';return `<section class="section card decision"><div class="eyebrow">Coaching decision</div><h3>${esc(d.title)}</h3><p>${esc(d.body)}</p><div class="decision-actions">${d.choices.map((c,i)=>`<button class="btn secondary" data-action="decision" data-index="${i}">${esc(c.label)}</button>`).join('')}</div></section>`}
function week(){const s=S.state.season;if(s.phase==='OFFSEASON')return hero()+decision()+`<section class="section card"><b>Offseason checkpoint</b><p class="small muted">The season history is saved. Start the next year when you are ready.</p><button class="btn primary wide" data-action="new-season">Start ${s.year+1} season</button></section>`;const g=S.userGame(),opp=g?(g.away===S.state.coach.team?g.home:g.away):'BYE';return hero()+decision()+`<section class="section"><div class="section-head"><h2>Week plan</h2><span>${opp==='BYE'?'Bye week':'vs '+opp}</span></div><div class="card">${S.DAYS.map((d,i)=>`<div class="dayrow ${i===s.day?'current':''}"><b class="small">${d.slice(0,3)}</b><span class="small">${['Recovery · medical · grades','Opponent scout · game plan','Base install · early downs','Third down · protection','Red zone · two minute','Walkthrough · roles','Game day'][i]}</span>${i===s.day?'<span class="pill">NOW</span>':''}</div>`).join('')}</div></section><section class="section"><div class="actions"><button class="btn primary" data-action="advance-day">Advance Day</button>${s.day<6?'<button class="btn secondary" data-action="game-day">Advance to Game Day</button>':''}</div></section><section class="section"><div class="section-head"><h2>Coaching log</h2><span>Important developments only</span></div><div class="card">${S.state.logs.slice(0,12).map(x=>`<div class="logline"><b>${esc(x.title)}</b><div class="muted">${esc(x.text)}</div></div>`).join('')||'<div class="small muted">No notable events yet.</div>'}</div></section>`}
function usage(){const t=S.state.league[S.state.coach.team],skill=t.roster.filter(p=>p.status==='ACT'&&['RB','WR','TE','OT','OG','C','OL'].includes(p.pos));return hero()+`<section class="section"><div class="section-head"><h2>Practice emphasis</h2><span>Separate from Sunday usage</span></div><div class="card">${skill.map(p=>{const opts=['OT','OG','C','OL'].includes(p.pos)?['Balanced','Pass Protection','Run Blocking']:['WR','TE'].includes(p.pos)?['Balanced','Route Detail','Hands']:['Balanced'];return `<div class="role-grid"><div><b class="small">${esc(p.name)}</b><div class="tiny muted">${p.pos} · ${p.practiceReps}% practice reps</div></div><select data-action="focus" data-id="${p.id}">${opts.map(o=>`<option ${o===p.practiceFocus?'selected':''}>${o}</option>`).join('')}</select></div>`}).join('')}</div></section><section class="section"><div class="section-head"><h2>Gameday roles</h2><span>Targets emerge from the play</span></div><div class="card">${skill.filter(p=>['WR','TE','RB'].includes(p.pos)).map(p=>`<div class="role-grid"><div><b class="small">${esc(p.name)}</b><div class="tiny muted">${p.pos} · confidence ${Math.round(p.confidence)}</div></div><select data-action="role" data-id="${p.id}">${['Featured','Starter','Rotation','Package','Emergency'].map(o=>`<option ${o===p.role?'selected':''}>${o}</option>`).join('')}</select></div>`).join('')}</div></section><section class="section"><div class="section-head"><h2>Roster</h2><span>Tap a player for his file</span></div><div class="card">${t.roster.filter(p=>p.status==='ACT').map(p=>`<div class="player" data-action="player" data-id="${p.id}"><div><div class="name">${esc(p.name)}</div><div class="meta">${p.pos} · ${esc(p.role)} · fatigue ${Math.round(p.fatigue)}</div></div><div class="grade">${p.overall}</div></div>`).join('')}</div></section>`}
function teamTotals(g){const us=S.state.coach.team,x=g.teamStats[us];return `<section class="section"><div class="section-head"><h2>Live team totals</h2><span>This game only</span></div><div class="card grid3">${[['Plays',x.plays],['Yards',x.yards],['Turnovers',x.turnovers],['Punts',x.punts],['FG',x.fgMade+'/'+x.fgAtt],['Sacks',x.sacksAllowed]].map(([a,b])=>`<div class="metric"><b>${b}</b><span>${a}</span></div>`).join('')}</div></section>`}
function table(title,heads,rows){return `<section class="section"><div class="section-head"><h2>${title}</h2><span>Full game book</span></div><div class="card table-wrap"><table class="table"><thead><tr>${heads.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>${rows.join('')}</tbody></table></div></section>`}
